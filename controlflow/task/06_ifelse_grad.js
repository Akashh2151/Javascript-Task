function condition(a,b,c,d,e,f,g,h,i) {
    var age=18
    if(a>=age){
        console.log(`Your age: ${a} so your eligible for vote `);
    }
    else{
        console.log(`Your age: ${a} so your not eligible for vote `);
    }
    if(a>=age){
        console.log(`Your age: ${b} so your eligible for vote `);
    }
    else{
        console.log(`Your age: ${b} so your not eligible for vote `);
    }
    if(c>=age){
        console.log(`Your age: ${c} so your eligible for vote `);
    }
    else{
        console.log(`Your age: ${c} so your not eligible for vote `);
    }
    if(d>=age){
        console.log(`Your age: ${d} so your eligible for vote `);
    }
    else{
        console.log(`Your age: ${d} so your not eligible for vote `);
    }
    if(e>=age  ){
        console.log(`Your age: ${e} invalid data`);
    }
    else{
        console.log(`Your age: ${e} invalid data`);
    }
    if(f>=120){
        console.log(`Your age: ${f} invalid data `);
    }
    else{
        console.log(`Your age: ${f} invalid data`);
    }
    if(g>=age || g==0){
        console.log(`Your age: ${g} invalid data `);
    }
    else{
        console.log(`Your age: ${g} invalid data `);
    }
    if(h>=age){
        console.log(`Your age: ${h} write correct value `);
    }
    else{
        console.log(`Your age: ${h} write correct value `);
    }
    if(i>=age){
        console.log(`Your age: ${i} write correct value`);
    }
    else{
        console.log(`Your age: ${i} write correct value `);
    }
    
}
condition(45,17,8,20,-10,200,0,undefined,null)